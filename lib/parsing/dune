(menhir
 (modules parser)
 (flags --dump --strict --explain))

(library
 (name parsing)
 (modules parser lexer ast lex_and_parse)
 (libraries common sedlex menhirLib)
 (preprocess
  (pps sedlex.ppx)))
